This folder is where your photos go. The site is already wired up to use
them automatically — you just need to add the files with these exact names:

  christian-headshot.jpg
      Your photo for the "Meet Your Instructor" section.
      A vertical (portrait-orientation) photo works best.
      Until this file exists, that section shows a simple placeholder mark
      instead of a broken image — so the site looks fine either way.

That's the only photo the site currently expects. If you'd like to add more
later (class photos, a logo, etc.), just let your website builder know and
it can wire up more spots for you.

Delete this text file once you no longer need it — it has no effect on the
website either way.
